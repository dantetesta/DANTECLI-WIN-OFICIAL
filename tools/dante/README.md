# dante (CLI)

Placeholder do CLI Named-Pipe que conversa com o app `dante`.

- F0: só este README. NÃO entra no build (o `CMakeLists.txt` raiz não faz
  `add_subdirectory(tools/dante)`).
- Futuro: cliente de linha de comando que envia comandos ao app via Named Pipe
  (Windows) e fallback equivalente fora do Windows.
